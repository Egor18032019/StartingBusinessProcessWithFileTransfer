📌 Проблемы и несоответствия в документации RunaWFE REST API

1. В документации (runawfe.ru/RestApiDeveloperGuide):
  На скриншоте img.png показано:
  name передаётся в URL как query-параметр:
  https://runawfe.ru/RestApiDeveloperGuide
  ![img.png](img.png)
  тут name передается параметр в строку запроса (URL),
  ![img_1.png](img_1.png)
  -F указывает на отправку multipart/form-data
  Один и тот же параметр описан двумя несовместимыми способами.
  Это вводит в заблуждение
  По исходному коду контроллера (@RequestParam String name) — правильный способ: query-параметр в URL

2. ❌ API не принимает "чистый" JSON для файловых переменных
  Не принимает чистый json - требует WfeFileVariable
  ![img_3.png](img_3.png)
  ![img_4.png](img_4.png)
  public class ForDraft - попробывал передавать файл при закрытии тоже не работает

3. ❌ Отсутствие публичной зависимости
   Где скачать зависимость ? В документации нет, тут тоже нет https://mvnrepository.com/
   Если собрать то где бы посмотреть инструкцию как собирать
    <dependency>
    <groupId>ru.runa.wfe</groupId>
    <artifactId>wfe-restapi</artifactId>
    <classifier>client</classifier>
    </dependency>

4.  ❌ Некорректное описание эндпоинта
  Базовый урл - /task (например, для localhost - localhost/restapi/task)

    Отвечает за работу с задачами, основные методы:
    /my - получение собственных задач (пользователя, JWT которого передается в Authorization) — не работает @PostMapping("
    my")

* В коде

  public WfePagedList<WfeTask> getMyTasks(@AuthenticationPrincipal AuthUser authUser, @RequestBody WfePagedListFilter
  filter) {
  List<WfTask> tasks = taskLogic.getMyTasks(authUser.getUser(), filter.toBatchPresentation(ClassPresentationType.TASK));
  return new WfePagedList<>(tasks.size(), Mappers.getMapper(WfeTaskMapper.class).map(tasks));
  }